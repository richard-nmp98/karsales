query getListQuestionByType($input: QuestionGroupByTypeInput!) {
	getListQuestionByType(input: $input) {
		answers {
			content
			correctAnswer
			id
			no
			parentNo
			questionId
		}
		answerType
		content
		explainAnswer
		id
		imageExplainAnswer
		images
		isActive
		no
		type
	}
}
